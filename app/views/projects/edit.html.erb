<div class="card">

  <div class="card-header">
    <h2 class="card-title d-flex justify-content-between mb-0">
      <div>
        <span>Edit Project</span>
      </div>

      <div>
        <%= link_to 'Delete', @project, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure?'} %>
      </div>
    </h2>
  </div>

  <div class="card-body">

    <%= render 'form', project: @project %>

  </div>

</div>

<%= link_to 'Back', project_path, class: 'btn btn-secondary mt-3 mb-3' %>
